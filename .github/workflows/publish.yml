name: Publish MSI as NuGet Package

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    
    - name: Setup NuGet
      uses: NuGet/setup-nuget@v1.0.5
    
    - name: Pack NuGet package
      run: |
        $baseVersion = "0.0.2"
        $buildNumber = $env:GITHUB_RUN_NUMBER
        $fullVersion = "$baseVersion-$buildNumber"
        (Get-Content MyMsiPackage.nuspec) `
          -replace '0.0.2-18', $fullVersion `
          -replace 'GopherSecurity-windows-0.0.2-18-amd64-release.msi', "GopherSecurity-windows-$fullVersion-amd64-release.msi" |
        Set-Content GopherPackage.nuspec
        nuget pack GopherPackage.nuspec
    
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
    
    - name: Add GitHub NuGet Source
      run: dotnet nuget add source --username ${{ github.actor }} --password ${{ secrets.GITHUB_TOKEN }} --store-password-in-clear-text --name github "https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json"
    
    - name: Push to GitHub Packages
      run: dotnet nuget push *.nupkg --source "github" --api-key ${{ secrets.GITHUB_TOKEN }}